import { CensorshipPipe } from './censorship.pipe';

describe('CensorshipPipe', () => {
  it('create an instance', () => {
    const pipe = new CensorshipPipe();
    expect(pipe).toBeTruthy();
  });
});
